"""
Paleta de colores y constantes de diseño para Simplex.
Tema: Blanco/Gris con Verde Marino (Teal) y Azul Cielo.
"""

# ── Fondos ────────────────────────────────────────────────────────────────────
BG_APP        = "#F0FDFA"   # Teal muy claro — fondo general
BG_SIDEBAR    = "#FFFFFF"   # Blanco — sidebar
BG_CONTENT    = "#F0FDFA"   # Teal blanquecino — área de contenido
BG_CARD       = "#FFFFFF"   # Blanco — tarjetas / paneles
BG_INPUT      = "#FFFFFF"   # Blanco — campos de entrada
BG_CODE       = "#F7FDFB"   # Blanquecino teal — textos monospace
BG_ROW_EVEN   = "#FFFFFF"   # Blanco — filas pares de tabla
BG_ROW_ODD    = "#F0FDFA"   # Teal muy claro — filas impares
BG_HEADER_TBL = "#CCFBF1"   # Teal suave — cabecera tabla
BG_ZJ_ROW     = "#D5F5F0"   # Teal claro — filas Zj/Cj-Zj
BG_SEPARATOR  = "#E2E8F0"   # Gris medio — separadores

# ── Colores primarios ─────────────────────────────────────────────────────────
EMERALD       = "#0D9488"   # Verde marino (teal) principal
EMERALD_DARK  = "#0F766E"   # Verde marino oscuro (hover)
EMERALD_LIGHT = "#CCFBF1"   # Verde marino muy claro (fondos)
EMERALD_MID   = "#5EEAD4"   # Verde marino medio (acentos)

SKY           = "#06B6D4"   # Cyan principal (complemento)
SKY_DARK      = "#0E7490"   # Cyan oscuro (hover)
SKY_LIGHT     = "#CFFAFE"   # Cyan muy claro (fondos)
SKY_MID       = "#67E8F9"   # Cyan medio

# ── Textos ────────────────────────────────────────────────────────────────────
TEXT_PRIMARY   = "#1E293B"  # Casi negro — texto principal
TEXT_SECONDARY = "#475569"  # Gris medio — texto secundario
TEXT_MUTED     = "#94A3B8"  # Gris claro — texto desactivado
TEXT_WHITE     = "#FFFFFF"  # Blanco puro
TEXT_EMERALD   = "#115E59"  # Verde marino oscuro — texto sobre fondo claro
TEXT_SKY       = "#164E63"  # Cyan oscuro — texto sobre fondo claro

# ── Resaltados de tabla ───────────────────────────────────────────────────────
COLOR_PIVOT    = "#F97316"  # Naranja — elemento pivote
COLOR_PIVOT_BG = "#FFF7ED"  # Naranja muy claro — fondo pivote
COLOR_ENT_BG   = "#CCFBF1"  # Teal claro — columna entrante
COLOR_ENT_FG   = "#115E59"  # Teal oscuro — texto columna entrante
COLOR_SAL_BG   = "#FEF9C3"  # Amarillo claro — fila saliente
COLOR_SAL_FG   = "#713F12"  # Marrón — texto fila saliente
COLOR_POSITIVE = "#0D9488"  # Teal — Cj-Zj positivo (maximización)
COLOR_NEGATIVE = "#EF4444"  # Rojo — Cj-Zj negativo
COLOR_NEUTRAL  = "#94A3B8"  # Gris — Cj-Zj cero

# ── Estados ───────────────────────────────────────────────────────────────────
STATE_OPTIMAL  = "#0D9488"  # Verde marino
STATE_MULTIPLE = "#F59E0B"  # Ámbar
STATE_UNBOUNDED= "#F97316"  # Naranja
STATE_INFEASIBLE="#EF4444"  # Rojo

# ── Bordes y sombras ──────────────────────────────────────────────────────────
BORDER_LIGHT  = "#E2E8F0"   # Borde suave
BORDER_MEDIUM = "#CBD5E1"   # Borde medio
BORDER_STRONG = "#94A3B8"   # Borde fuerte

# ── Sidebar ───────────────────────────────────────────────────────────────────
SIDEBAR_ACTIVE_BG  = "#CCFBF1"   # Teal claro — ítem activo
SIDEBAR_ACTIVE_FG  = "#115E59"   # Teal oscuro — texto activo
SIDEBAR_HOVER_BG   = "#F0FDFA"   # Teal muy claro — hover
SIDEBAR_ICON_COLOR = "#0D9488"   # Verde marino — iconos

# ── Fuentes ───────────────────────────────────────────────────────────────────
FONT_FAMILY_UI   = "Segoe UI"
FONT_FAMILY_MONO = "Consolas"
FONT_SIZE_XS  = 9
FONT_SIZE_SM  = 11
FONT_SIZE_MD  = 12
FONT_SIZE_LG  = 14
FONT_SIZE_XL  = 16
FONT_SIZE_2XL = 20
FONT_SIZE_3XL = 28
FONT_SIZE_HERO= 40
